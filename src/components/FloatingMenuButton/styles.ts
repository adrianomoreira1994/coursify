import {
  StyleSheet
} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 40,
    height: 40,
    borderRadius: 25,
    backgroundColor: '#3CC6AA',
    justifyContent: 'center',
    alignItems: 'center'
  }
})