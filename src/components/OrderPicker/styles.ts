import {
  StyleSheet,
  Platform
} from 'react-native';
import { theme } from '../../theme';

export const styles = StyleSheet.create({
  containerPicker: {
    backgroundColor: "#FFF",
    borderColor: theme.gray_700,
    borderWidth: 1,
    borderRadius: 10,
    flex: 1,
  }
})